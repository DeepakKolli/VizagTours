import React from 'react';
import TourPackages from '../components/sections/TourPackages';

const Packages = () => {
  return (
    <div style={{ paddingTop: '40px', backgroundColor: 'var(--bg-white)' }}>
      <TourPackages />
    </div>
  );
};

export default Packages;
